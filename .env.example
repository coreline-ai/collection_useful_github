# Optional: GitHub API rate-limit improvement
VITE_GITHUB_TOKEN=
VITE_GITHUB_TIMEOUT_SECONDS=12

# Optional: AI-based Korean translation (GLM priority, OpenAI fallback)
GLM_API_KEY=
GLM_BASE_URL=https://api.z.ai/api/coding/paas/v4
GLM_MODEL=glm-4.7
GLM_TIMEOUT_SECONDS=30

# Optional fallback provider
VITE_OPENAI_API_KEY=
VITE_OPENAI_MODEL=gpt-4.1-mini
VITE_OPENAI_TIMEOUT_SECONDS=30

# Optional: PostgreSQL snapshot sync API (server)
VITE_POSTGRES_SYNC_API_BASE_URL=http://localhost:4000
